-- PostgreSQL Performance Assignment
-- Generate 2 million orders, analyze a slow query,
-- add a targeted index, test isolation levels,
-- and document PgBouncer transaction pooling.

-- Step 1: Generate a Big Table
CREATE TABLE orders (
    id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    customer_id INT,
    amount NUMERIC(10,2),
    status TEXT,
    created_at TIMESTAMPTZ DEFAULT now()
);

INSERT INTO orders (customer_id, amount, status, created_at)
SELECT
    (random() * 50000)::int,
    (random() * 500)::numeric(10,2),
    (ARRAY['pending', 'shipped', 'delivered'])[ceil(random() * 3)],
    now() - (random() * 365)::int * interval '1 day'
FROM generate_series(1, 2000000);

ANALYZE orders;


-- Step 2: Measure the Slow Query
EXPLAIN (ANALYZE, BUFFERS)
SELECT customer_id, SUM(amount)
FROM orders
WHERE status = 'pending'
  AND created_at > now() - interval '30 days'
GROUP BY customer_id
ORDER BY SUM(amount) DESC
LIMIT 10;


-- Step 3: Add a Targeted Index and Re-Measure
CREATE INDEX idx_pending_recent
ON orders (created_at DESC, customer_id)
WHERE status = 'pending';

EXPLAIN (ANALYZE, BUFFERS)
SELECT customer_id, SUM(amount)
FROM orders
WHERE status = 'pending'
  AND created_at > now() - interval '30 days'
GROUP BY customer_id
ORDER BY SUM(amount) DESC
LIMIT 10;


-- Step 4: Observe Isolation Levels

-- Session 1:
-- BEGIN;
-- SELECT amount FROM orders WHERE id = 1;

-- Session 2:
-- UPDATE orders SET amount = 9999 WHERE id = 1;
-- COMMIT;

-- Session 1 again:
-- SELECT amount FROM orders WHERE id = 1;
-- COMMIT;


-- Repeatable Read test:
-- Session 1:
-- BEGIN TRANSACTION ISOLATION LEVEL REPEATABLE READ;
-- SELECT amount FROM orders WHERE id = 1;

-- Session 2:
-- UPDATE orders SET amount = 7777 WHERE id = 1;
-- COMMIT;

-- Session 1:
-- SELECT amount FROM orders WHERE id = 1;
-- COMMIT;


-- Step 5: PgBouncer Configuration
-- PgBouncer was configured in transaction pooling mode.
--
-- [databases]
-- bootcamp = host=172.29.80.1 port=5432 dbname=bootcamp
--
-- [pgbouncer]
-- pool_mode = transaction
-- max_client_conn = 1000
-- default_pool_size = 20
-- listen_port = 6432
-- listen_addr = 127.0.0.1
--
-- PgBouncer connection was successfully tested through port 6432.
-- PostgreSQL backend connection reported port 5432.
